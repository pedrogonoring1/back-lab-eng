import 'reflect-metadata';

// import { expect } from 'chai';
// import request from 'supertest';

// import { getTestContainer } from '../helpers/g etTestContainer';
// import Server from '../../src/lib/Server';

describe('UserController', () => {
  // const server = getTestContainer().get(Server).app;

  // describe('POST /user', () => {
  //   const body = {
  //     data: {
  //       description: 'yessir',
  //     },
  //   };

  //   describe('when the correct params are provided', () => {
  //     it('creates the user', async () => {
  //       const res = await request(server).post('/user').send(body);

  //       expect(res.status).to.eql(201);
  //       expect(res.body).to.deep.eq({
  //         data: {
  //           id: res.body.data.id,
  //           adopter: res.body.data.adopter,
  //           name: res.body.data.name,
  //           cpfOrCnpj: res.body.data.cpfOrCnpj,
  //           address: res.body.data.address,
  //           birthDate: res.body.data.birthDate,
  //           phone: res.body.data.phone,
  //           email: res.body.data.email,
  //           password: res.body.data.password,
  //         },
  //       });
  //     });
  //   });
  // });
});
